package com.example.demo.api.tweeter.domain

import org.springframework.data.elasticsearch.repository.ElasticsearchRepository
import org.springframework.stereotype.Repository
import java.util.*

/*
@Repository
interface EsAuthorRepository : ElasticsearchRepository<Author, UUID> {
    fun getById(id: UUID): Optional<Author>
}


@Repository
interface EsTweetRepository : ElasticsearchRepository<Tweet, UUID> {
    fun getById(id: UUID): Optional<Tweet>
}
        */